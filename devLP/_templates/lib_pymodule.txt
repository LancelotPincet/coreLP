#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# Date          : template_date
# Author        : Lancelot PINCET
# GitHub        : https://github.com/LancelotPincet

"""
Create new module for template_name
"""



# %% Libraries
from devlp import path
from datetime import datetime
import os
import json



# %% Main function
def main() :
    print('\nRunning add_module :')

    # Ask for script name
    already_exists = True
    while already_exists :
        name = input('     New module name ? >>> ')
        module_path = path.parent / f'libsLP/template_name/src/template_lowername/modules/{name}_LP'
        if module_path.exists() :
            print('     This module already exists :/')
        else :
            already_exists = False
    description = input('     what will the module do ? >>> ')
    date = datetime.now().strftime("%Y-%m-%d")

    # Print informations
    print(f'     name : {name}')
    print(f'     description : {description}')
    print(f'     date : {date}')
    os.mkdir(module_path)

    # Write python module
    python_path = path / '_templates/lib_module.txt'
    newpython_path = module_path / f'{name}.py'
    with open(python_path, "r") as file :
        string = file.read()
    string = string.replace('template_modulename', name)
    string = string.replace('template_moduledate', date)
    string = string.replace('template_moduledescription', description)
    string = string.replace('template_modulelib', template_name)
    string = string.replace('template_modulelowerlib', template_lowername)
    with open(newpython_path, "w") as file :
        file.write(string)
    with open(module_path / "__init__.py", "w") as file :
        file.write("")
    print('     Module file written [to complete]')

    # Add test
    python_path = path / '_templates/lib_test.txt'
    newpython_path = module_path / f'test_{name}.py'
    with open(python_path, "r") as file :
        string = file.read()
    string = string.replace('template_modulename', name)
    string = string.replace('template_moduledate', date)
    string = string.replace('template_moduledescription', description)
    string = string.replace('template_modulelib', template_name)
    string = string.replace('template_modulelowerlib', template_lowername)
    with open(newpython_path, "w") as file :
        file.write(string)
    print('     Test file written [to complete]')

    # Add module to json file
    modjson = {}
    modjson['module'] = f"{name}_LP/{name}"
    modjson['object'] = name
    modjson['description'] = description
    modjson['date'] = date
    json_path = path.parent / f'libsLP/template_name/src/template_lowername/modules.json'
    with open(json_path, "r") as file :
        data = json.load(file)
    data[name] = modjson
    with open(json_path, "w") as file :
        json.dump(data, file, indent=4, sort_keys=True)
    print("     Module json updated")

    # End
    print('add_module finished!\n')



# %% Main function run
if __name__ == "__main__":
    main()